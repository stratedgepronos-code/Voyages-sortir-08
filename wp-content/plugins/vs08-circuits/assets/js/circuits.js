/**
 * VS08 Circuits — Front
 */
(function($){
    'use strict';
    // Filtres : conserver les paramètres dans l’URL (déjà géré par le formulaire GET)
    $(document).ready(function(){
        // Optionnel : soumission des filtres sans rechargement (AJAX) — à activer si besoin
        // $('#vs08c-filters select').on('change', function(){ $('#vs08c-filters').submit(); });
    });
})(jQuery);
